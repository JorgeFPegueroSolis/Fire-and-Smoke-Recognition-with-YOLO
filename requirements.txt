# Fire Detection Agent - Dependencies
# Install with: pip install -r requirements.txt
# Note: This project is designed to run on Google Colab.
# Some packages (google-colab, kaggle) may behave differently in local environments.

# Core ML framework
ultralytics

# Computer vision
opencv-python

# HTTP requests (Telegram API & video download)
requests

# Image processing (frame display in notebook)
Pillow

# Data visualization (frame display in notebook)
matplotlib

# Numerical computing (used internally by ultralytics and opencv)
numpy
